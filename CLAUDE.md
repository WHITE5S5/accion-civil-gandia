# Acción Civil Gandia — Web institucional

> ⚠️ **RENOMBRADO 2026-07-07:** la Home ya NO es `Acción Civil Gandia.dc.html` → ahora es **`index.html`** (requisito Netlify + URLs sin tildes/espacios). La print es `home-print.dc.html`. Los 264 enlaces internos ya están actualizados. Toda mención antigua al archivo con tilde en memorias/documentos se refiere a `index.html`.
> **Hosting temporal: Netlify** (drag&drop o repo GitHub) hasta tener dominio; `_headers`/`_redirects` son formato nativo Netlify. En Fase 1 las functions van en `netlify/functions/` (no `functions/`).
> **Deploy:** ejecutar `python scripts/make_deploy.py` → genera `deploy_netlify/` (solo archivos publicables, ~17,5 MB) → arrastrar ESA carpeta a Netlify. NUNCA subir la raíz del proyecto (contiene specs, auditorías, scripts y uploads privados). Regenerar tras cada cambio.

## Antes de hacer NADA

1. Leer `F:\WHITE_OS\08_MEMORIA\CLAUDE_MEMORY\CLAUDE_ACCION_CIVIL.md` — contiene TODO el contexto: stack, patrones, design tokens, nav, footer, estado de páginas, pendientes, errores conocidos.
2. Leer la sección "Errores a no repetir" de este archivo (abajo).
3. Dev server: `python -m http.server 8091` desde esta carpeta. Verificar que corre antes de abrir páginas.

## Auditoría y roadmap backend (2026-07-07, estado revisado 2026-07-08)

- `AUDITORIA_Y_ROADMAP_BACKEND.md` — auditoría del frontend (8,4/10) + roadmap de 6 fases (visión general).
- `SPEC_BACKEND_DETALLADO.md` — **spec ejecutable**: esquema SQL completo, políticas RLS, contratos de API endpoint por endpoint, eventos Stripe uno a uno, `_headers`/`_redirects` literales, criterios de aceptación por fase.
- Copia visual en el vault: `01_PROYECTOS/ACCION_CIVIL/`.

### Estado real del backend (2026-07-08) — el código va MUY por delante del roadmap

- **F1 formularios — 100% código**: `contacto`, `newsletter`+`newsletter-confirm` (double opt-in), `propuesta`, `assets/acg-forms.js` en 67 páginas, 20/20 tests (`node scripts/test_functions.mjs`). Sin claves → 503 controlado.
- **F2 contenido — 100% código**: 5 migraciones SQL (`supabase/migrations/`), 10 funciones de contenido (`posts`, `events`, `campaigns`, `actuaciones`, `equipo`, `home` + detalles), seeds y `scripts/sync_phase2_content.mjs`; 12 páginas ya hacen fetch a `/api/`. **Sin Directus**: panel `/admin` propio (`admin.mjs`, contraseña + token HMAC 12h, audit_log).
- **F3 participación — ~75% (2026-07-08 tarde, DESPLEGADO)**: migración 006 aplicada (tabla `messages` + consents tipo `marketing`). `chat.mjs` (`/api/chat`: historial, envío, moderación) + `assets/acg-chat.js` activa el chat de Comunidad con sesión. `/cuenta` con login Google (OAuth capturado del hash), checkbox marketing opt-in (desmarcado → consents + Brevo por idioma). Panel `/admin`: centrado (`.page`), i18n ES/VA (toggle en topbar, `F_VA`/`TT`) y vista previa live con pestañas ES/VA en posts/events/campaigns/actuaciones/equipo. Falta de F3: 2FA moderadores, conectar Participacion a proposals reales (decisión: cuando haya ≥6), atribución individual en admin.
- **F4 Stripe — 100% código**: `donaciones`, `afiliacion`, `stripe-webhook` + 10 tests (LO 8/2007 codificada).
- **F5 parcial**: `denuncias` (Ley 2/2023), `tesoreria` → Transparencia, `audit_log`, rate limiting, `docs/tests-rls.md`.
- **✅ Google OAuth configurado (2026-07-08):** proyecto GCP `steady-cat-478811-k0` (app "Acción Civil Gandia", publicada en producción), cliente "Web Accion Civil" con callback a Supabase; provider Google Enabled en Supabase con redirect URLs (`accioncivilgandia.netlify.app/**` y `localhost:8091/**`). Flujo verificado hasta el selector de cuenta de Google. OJO: los secretos de cliente GCP solo se pueden copiar al crearlos.
- **Panel admin v2 (2026-07-09):** autotraducción ES↔VA al guardar (Apertium `spa|cat_valencia`, best-effort con copia si falla, en `autotraducir()` de admin.mjs), subida de imágenes drag&drop/móvil (redimensiona a 1600px webp en cliente → action `upload` → bucket público `media` de Supabase Storage; CSP img-src incluye `*.supabase.co`), enlace Brevo renombrado. Botón "Entrar/Mi cuenta" en el header de toda la web vía `acg-forms.js` (busca `margin-left: auto` CON espacio — el DOM serializa así). ⚠ `/assets/*.js` ahora con cache de 5 min (antes immutable 1 año — los usuarios no recibían actualizaciones); scripts referenciados con `?v=2`: al cambiar acg-*.js, subir la versión en las 69 páginas.
- **✅ F1 y F2 OPERATIVAS en producción (verificado 2026-07-08):** claves de Resend, Brevo, Supabase y admin configuradas en Netlify; migraciones corridas; `/api/posts|events|home|tesoreria` devuelven 200 con `source:supabase`. Claves Supabase también en `.env` local (proyecto vinculado vía `.netlify/state.json`). Pendientes de claves: solo Turnstile (opcional) y Stripe/ENCRYPTION_KEY (F4).

### Cierre de roadmap 2026-07-09e (F3 completada, F4 lista, F5 documentada)

- **Comentarios operativos**: `PropuestaCiudadana.dc.html` ahora tiene compositor (textarea `.acg-coment-input` + botón "Enviar comentario") cableado en `acg-forms.js` (`comentar()`) → POST `/api/comentarios` (backend ya existía, exige sesión → popup; cola de moderación). `acg-forms.js` subido a **?v=8** en las 69 páginas.
- **FichaEvento y DetalleCampana dinámicas**: `_loadEvent()`/`_loadCampaign()` por slug de `/agenda/:slug` y `/campanas/:slug` (catch-all añadidos en `_redirects`) → `/api/event-detail` y `/api/campaign-detail` (ya existían). Fallback estático intacto (el `programa`/`objetivos`/`timeline` no están en el CMS y se conservan). Mismo patrón que DetallePublicacion. Verificado en navegador: renderizan sin regresión.
- **2FA TOTP en el panel admin** (RFC 6238): activable con env **`ADMIN_TOTP_SECRET`** (base32). Retrocompatible: sin esa env, sigue solo con contraseña (no bloquea). Generar secreto+QR: **`node scripts/gen_totp_secret.mjs`** (y `--test` valida el vector oficial RFC). Helpers exportados desde admin.mjs; input de código añadido al login del panel. Verificado: vector RFC 287082 OK + round-trip.
- **Stripe F4**: código correcto y con guard `503` sin claves; tests añadidos a la suite (**37 OK, 0 FAIL** con `node scripts/test_functions.mjs`, incluye validación DNI/NIE LO 8/2007). Claves que faltan para activar (Netlify env): `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_CUOTA_{MENSUAL,ANUAL,REDUCIDA}`, `ENCRYPTION_KEY`, `APP_BASE_URL`. Pendiente (tras crear cuenta Stripe): construir/activar las páginas de donación y afiliación con sus formularios legales + Customer Portal.
- **F5**: rate limiting cubre todos los endpoints de escritura (lecturas GET van cacheadas por CDN); denuncias (Ley 2/2023) operativas en `/denuncias`; `audit_log` en admin/chat/cuenta/webhook. **Registro de Actividades de Tratamiento (RGPD art. 30)** redactado en `docs/RGPD_REGISTRO_ACTIVIDADES.md`.
- **Tras estos cambios**: regenerar `deploy_netlify/` con `python scripts/make_deploy.py` antes de subir a Netlify.

### BD: retención de analítica + backup (2026-07-18g)
- **Migración 021** (`analytics_mensual` + índices calientes) — ✅ APLICADA (2026-07-18, verificada por REST).
- **Retención**: `recordatorio-actos.mjs` (diaria 07:00 UTC) consolida eventos de `analytics_events` >90 días en `analytics_mensual` (mes/tipo/clave/hits) y borra SOLO lo consolidado (por id, tope 5.000/día). El dashboard usa ventanas ≤30 días → no le afecta. Sin la migración 021, el rollup falla con `bad_table` y NO borra nada (seguro).
- **Backup semanal**: `node scripts/backup_supabase.mjs` → todas las tablas (descubiertas por OpenAPI) + auth.users en JSON.gz a `F:\Backups\AccionCivil\backup-YYYY-MM-DD\` (rota, conserva 8). Programado en el Programador de tareas de Windows ("ACG Backup Supabase", domingos 03:30). Restauración = migraciones (esquema) + estos JSON (datos).

### Frescura de datos (caché) — 2026-07-09f
- **Chat**: `acg-chat.js` sondea mensajes nuevos cada **4 s** (antes 20 s). Versionado `?v=6`.
- **Ahorro de créditos Netlify (2026-07-18)**: TODO el polling se PAUSA con la pestaña oculta (`document.hidden`) y refresca al instante al volver (`visibilitychange`). Intervalos visibles: chat 4s (intacto), Comunidad broadcasts 15s, Home 30s, Participación 30s, DetalleActuacion 60s. Netlify free = 300 créditos/mes y AL AGOTARSE DEJA DE SERVIR → nunca añadir un poller sin pausa por visibilidad. acg-chat `?v=9`.
- **Polling unificado en TODAS las páginas de contenido (2026-07-18)**: patrón único obligatorio →
  `if(!this._acgPoll){ this._acgPoll=setInterval(()=>{ if(!document.hidden) this._loadX(); },N); try{ document.addEventListener('visibilitychange',()=>{ if(!document.hidden) this._loadX(); }); }catch(e){} }`
  + guarda de firma en la respuesta (`const _t=await r.text(); if(this._acgSig===_t) return; this._acgSig=_t;` — o `JSON.stringify` del resultado) para NO re-renderizar/parpadear ni reinit de mapas si nada cambió. Se añadió polling a las que cargaban una sola vez: PropuestaCiudadana (comentarios/votos 7s), Propuestas 10s, DetallePublicacion/FichaEvento/DetalleCampana 12s, Categoria×5 15s, Conocenos/Equipo 25s, Participa/Tienda/Producto 20s, Historia 30s. Al quitar los early-return `if(this._xLang===lang && this.state.remoteX) return;` de los detalles, verificado que su `componentDidUpdate` NO llama a la carga (solo título/mapa con su propia guarda) → sin tormenta de fetches. Resultado: comentarios, propuestas, noticias, tienda… aparecen solos sin recargar. Regla: cualquier `_loadX` nuevo usa este patrón (pausa hidden + visibilitychange + firma), nunca un `setInterval` pelado.
- **APIs de contenido**: `cacheHeaders()` (en `lib/content-api.mjs`) ahora emite `cache-control: public, max-age=0, must-revalidate` (el navegador NUNCA sirve copia vieja) + `netlify-cdn-cache-control: s-maxage≤10s`. Antes era `max-age=300` → los cambios del admin (posts, tesorería, campañas, eventos…) tardaban hasta 5 min en verse. Ahora salen en **≤10 s**. NO volver a poner `max-age` largo en el `cache-control` estándar: el admin escribe todo por `admin.mjs` y la web pública lee por estas APIs; un TTL largo hace que parezca que "no se actualiza". El panel `/admin` lee con service_role sin caché → siempre fresco.

## Stack resumido

- **dc-runtime** (`support.js`) — NO editar. Archivos `.dc.html`, cada uno es una SPA con `<x-dc>`.
- **Patrón:** `class Component extends DCLogic` → `data(lang)` para datos estáticos + `renderVals()` para valores responsivos.
- **Templates:** `{{ var }}` (sin JS/ternarios), `<sc-for>`, `<sc-if>`, `<helmet>`.
- **Bilingüe:** `V(es, va)`. Idioma en `localStorage('acg_lang')`.
- **Responsive:** `isMob = s.device === 'mobile'`. Todo pre-computado en `renderVals()`.

## Crear una página nueva — checklist

1. Copiar estructura de una página similar existente (no empezar de cero).
2. `<helmet>`: title, description, canonical, OG, favicon, fonts, CSS (incluir `.acg-nav-item`, `.acg-dd`, `.acg-dd-link`).
3. Header con nav dropdown (copiar de `CLAUDE_ACCION_CIVIL.md` → "Nav data() completo").
4. Breadcrumb.
5. Contenido con hero navy gradient.
6. Footer 4 columnas (copiar datos de `CLAUDE_ACCION_CIVIL.md` → "Datos footer en data()").
7. Cambiar `col:'#0A2A5E', weight:700` en el nav item activo según la sección.
8. Actualizar `Sitemap.dc.html` si la página estaba como "Planificada" → cambiar a OK con href.

## Páginas pendientes (prioridad)

Ver lista completa en `CLAUDE_ACCION_CIVIL.md` → "Páginas pendientes".

Alta:
- Home (`index.html`) — añadir dropdowns al nav (estructura diferente, layout dual)
- Detalle de publicación (Actualidad)
- Ficha de evento (Agenda)
- Detalle de campaña (Campañas)

Media:
- Mapa de actuaciones + Barrios × 5 + Detalle de actuación (Acción en Gandia)

Baja:
- Categorías propuestas, Programa PDF, Buscador global, Perfil individual

### Sesión 2026-07-12 — Auditoría de practicidad + conexión total (autónoma)

- **Fix crítico `_redirects`**: `/propuesta-ciudadana` apuntaba a `Propuesta.dc.html` (demo vieja, nav de 3 ítems) → ahora a `PropuestaCiudadana.dc.html`; `/Propuesta.dc.html` → 301 a Participación. Refs de Ciudadano/Crear actualizadas.
- **Home conectada**: tendencias = top-3 de `/api/proposals` (votos/comentarios/estado reales, tarjetas con `data-pid` → votar desde la Home funciona), contadores de participación reales (fuera 1240/38512 fake), campaña destacada desde `/api/home` (38/100 real).
- **Popup newsletter**: cada 2 páginas + 12 s (o 50 % scroll), respiro mínimo 90 s, para al suscribirse (`acg_nl_ok`, también lo marca el footer). acg-forms **?v=16**.
- **/cuenta v2**: stats (propuestas/apoyos recibidos/comentarios/likes), mis comentarios con estado de moderación y enlace a la propuesta, propuestas con 👍👎💬, "Darme de baja". Backend `mis-datos` ampliado.
- **Likes en comentarios**: migración `014_comment_likes.sql` (⚠ PENDIENTE de ejecutar en SQL Editor), `/api/comentarios` con GET enriquecido + action `like` (degrada con `likesOn:false` si no existe la tabla), corazones en PropuestaCiudadana, delegación en acg-forms.
- **Equipo**: Conócenos muestra 6 en escritorio / 4 en móvil (antes 4 fijos). **Cargos corregidos** (BD + pendrive = verdad): Sebastián Mesa = Vicepresidente ejecutivo (foto `miembro-reig.webp`), Miguel Reig = Tesorero (foto `miembro-mesa.webp`) — estáticos de Conocenos/Equipo/Home/Perfiles igualados a la BD.
- **Pendrive E:\ integrado**: CIF real **G44754356** en AvisoLegal (antes G-00000000) y Privacidad; domicilio social AEAT (C/ Ciutat Comtal 6) + oficinas en AvisoLegal; **estatutos reales** (PDF 5,7 MB comprimido) subidos a Storage y enlazados en Transparencia; sección "Documentos" de Transparencia ya sin PDFs falsos (solo reales); 2 noticias reales con fotos del pendrive (Carrera de la Mujer 2026, Festa del Traguet 2026) + foto real en la de la Nit dels Ciris. Bucket `media` ahora admite PDF y 20 MB.
- **Admin CRM**: serie 30 días (1 query), toggle 7d/30d, embudo de conversión, feed "Actividad reciente" (action `actividad`), tooltips por hora en la curva.
- **Redes reales en footer (69 págs)**: Facebook (página oficial), Instagram/TikTok/YouTube `@accioncivilgandia`; Twitter/X eliminado (no existe). Los clicks ya se trackean (support.js → /api/hit).
- **Stripe operativo** (claves test en Netlify): donación 10/25/50/100/libre y afiliación 15/30/libre (≥30) generan Checkout real. Falta pasar a claves live.
- **Perf**: `support.js` con `defer` + `<style>x-dc{display:none}</style>` en las 69 páginas (antes bloqueaba el render).
- **Limpieza BD**: 2 propuestas basura ("adadadadadad") → rechazadas; usuario/comentario/voto/donación de prueba eliminados.

### 2026-07-12 (tarde) — Admin v3 + likes activados

- **Migración 014 APLICADA** (SQL Editor vía Chrome del usuario con sesión activa; Monaco: `monaco.editor.getModels()[0].setValue(sql)` + click Run). Likes de comentarios VIVOS en producción.
- **Analítica saneada**: support.js ya NO trackea desde localhost (el dev local contaminaba producción: las "400 visitas" del 12-jul eran la auditoría + 2 escaneos Lighthouse → borrados los eventos de ese día). Detección de red ENTRANTE en pageviews (referrer + utm_source/fbclid/igshid → columna `red`).
- **Dashboard v3**: selector de 7 días en la curva horaria (matriz `horas8` de 8 días en 1 query), tooltip interactivo con crosshair (nada de <title> nativo), "Páginas más vistas" con nombres legibles (mapa NOMBRES + slugs), panel "Redes: salen y entran" siempre con las 4 redes y contadores (clicks out + visitas in, hoy·7d), tarjetas de comunidad = total grande + "+N hoy" (fuera el formato confuso "1 / 7"), `.dash` a flex centrado (6→3+3, sueltos al centro — REGLA DE ORO escrita en el vault CLAUDE_PREFERENCIAS_USUARIO.md).
- Desplegado y verificado (script cliente del admin parsea OK en prod; support.js con redEntrante en prod).

### 2026-07-12 (noche) — Ronda 2: revisión del usuario, 15 frentes (desplegado)

- **Migración 015_interaccion.sql APLICADA** (SQL Editor vía Chrome): `campaign_supports`, `event_inscripciones` (+`events.inscribible`), `denuncia_mensajes` (+denuncias.user_id/email), `profiles.avatar_url`.
- **APIs nuevas**: `/api/campanas-apoyo` (GET counts+mios, POST toggle), `/api/inscripcion` (GET estado, POST alta+email Resend), `recordatorio-actos.mjs` (programada 07:00 UTC, recordatorio día antes), `/api/tesoreria?format=csv` (libro completo real) y `?lang=va` (categorías traducidas), `/api/home` con `stats` reales (afiliados/propuestas/barrios), `/api/comentarios` action `borrar` (propio) + avatar en GET, denuncias con hilo (action `mensaje`) y vínculo a cuenta (código por email), chat con avatar.
- **Home**: campaña sin votos falsos (progreso admin + apoyos reales + botón Apoyar `data-acg-camp` + compartir), acciones→/accion, actos→/agenda/:slug, tile transparencia con destinos reales, Únete en nav+footer (69 págs), caché local anti-FOUC (`acg_c_home_/props_<lang>`).
- **Detalle propuesta**: skeleton "Cargando…" con ?id (sin demo carril bici). Participación: caché + paginación real 6/pág (botones sc-if showPager). Propuestas: destacadas = top-3 ciudadanas de /api/proposals.
- **Campañas**: apoyar en featured/tarjetas/detalle (contador `data-camp-n`), DetalleCampana con imagen de fondo+timeline con hitos+docs solo reales; grids activas/noticias flex-centrados. normalizeCampaign expone objetivos/cronologia/docs (jsonb; formatos: objetivos=strings, cronologia={date,text}, docs={name,size,href}).
- **Agenda**: botón por evento según `inscribible` (Inscribirse / Más información, `data-slug`); modal de support.js reescrito → POST /api/inscripcion (sesión opcional); admin→Agenda con caja "Inscritos por acto" (action `inscripciones-list`) + checkbox inscribible en el form.
- **Transparencia**: tiers reales 15/30/libre + donación → modales acg-pay (script añadido), Descargar cuentas (CSV real), docs tiles centrados, compromisos = actuaciones REALES (stateKey), tesorería VA.
- **Denuncias**: aviso previo anónimo-vs-cuenta, código en grande + copiar, por email si hay sesión, hilo bidireccional (página con ?ver=CODIGO precarga; admin: denHiloBox bajo el form + email de aviso al informante).
- **Cuenta v3**: card 940px, avatar (Google auto / subida canvas 256→webp→storage avatares/), listas .lista con scroll, borrar comentario, denuncias propias, export "Copia legible (PDF)" (ventana print).
- **Global**: scroll restaurado al volver atrás (sessionStorage+back_forward, en support.js), popup newsletter no sale con sesión, compartir por aria-label, versiones acg-forms v17 / acg-chat v7.
- ⚠ Pendiente de pulir: `?lang=va` cache-first usa clave por idioma (ok); print CSS de programa-electoral sin reglas de salto aún.

### 2026-07-12 (cierre) — Stripe hacia live + multimedia del pendrive

- **Stripe**: la cuenta YA pertenece al correo del partido (login accioncivilgandia@gmail.com, titular "Alejandro", superadmin). Correo de la cuenta VERIFICADO (vía Gmail del partido, sesión u/1 en Chrome del usuario). Invitación enviada a nicosfb2016@gmail.com como **Administrador** (pendiente de aceptar). PENDIENTE del usuario: aceptar invitación + completar "Verificar tu empresa" (CIF G44754356, domicilio C/ Ciutat Comtal 6, IBAN ES25 2100 4564 3602 0019 7286, DNI del representante) → después ejecutar **`node scripts/stripe_go_live.mjs sk_live_XXX`** (crea producto+precios 15/30+webhook live e imprime los env:set) + deploy + pago de prueba de 1 €.
- **Multimedia integrado**: cartel real del candidato 2027 (webp 143KB) en sección nueva "El candidato" de /programa-electoral (grid 380px+texto, lema "Orgullo de Gandia"); 3 registros oficiales del Ayuntamiento (solicitud+respuesta aparcabicis E-RE-24266, solicitud WiFi Raval E-RE-24007) subidos a Storage docs/ y listados en Documentos de Transparencia (6 tiles = 3+3); tile "Último vídeo" de la Home → canal YouTube @accioncivilgandia. Himno/vídeos WhatsApp descartados (calidad insuficiente para la web).

### Stats públicas reales (2026-07-18)
- **Afiliados públicos = suscripciones activas de Stripe** (estado `active`/`baja_fin`), NO la tabla legacy `afiliados` (histórica, "pendientes de reafiliación"). `home.mjs statsReales()` cuenta Stripe; respaldo legacy solo si no hay `STRIPE_SECRET_KEY` o falla la llamada. Hoy → 2. Decisión del usuario: mostrar solo socios reales al día.
- **Participación (Participacion.dc.html)**: Propuestas = `recibidas` de `/api/proposals` (total real, no las 10 cargadas); Barrios = total real de `/api/barrios` (11), NO barrios distintos entre las propuestas cargadas (daba 6). Nuevos estados `remoteRecibidas`/`remoteBarrios`.
- **Únete (Participa.dc.html)** ya tiraba de `/api/home` stats; ahora 2/11/11 reales.

### Home móvil: header/footer igualados al resto (2026-07-18)
- **La Home móvil NO tiene `<header>`** (layout `acg-mob` propio) → acg-forms/acg-shop no inyectaban Únete/Cuenta/Carrito. Solución: la barra superior móvil lleva `id="acg-mobbar"` y `hostHeader()` (acg-forms) + el host del carrito (acg-shop) ahora incluyen `,#acg-mobbar`. Los pills se autoestilan (fondo propio) y a ≤1100px van solo-icono. acg-forms **?v=37**, acg-shop **?v=5** (solo bumpeado en index.html; el cambio es retrocompatible con el resto). Si otra página usa layout sin `<header>`, mismo hook.
- **Footer móvil de la Home** estaba recortado (logo+desc+social) → ampliado a las 4 secciones del estándar (marca+social, Explorar `footerNav1`, Legal `footerNav2`, Boletín con `.acg-nl-in`/`.acg-nl-btn`). El footer DESKTOP de la Home ya era idéntico al resto.
- **MapaActuaciones: flash de demo** = la clase `acg-sk` se aplicaba pero **no existía `.acg-sk` en el CSS de esa página** → las cifras demo se pintaban a color y luego se sustituían por las reales. Regla: si usas `skCls:'acg-sk'`, la página DEBE definir `.acg-sk` (ocultar texto + shimmer) Y bajo carga pintar placeholders en blanco, nunca la demo. Ahora `blankBarrios`/`blankStats` mientras `remote==null`.

### Carrito congelaba TODA la web al tener items (2026-07-18)
- **Bug**: `updateBadge()` en `acg-shop.js` hacía `el.textContent = String(c)` en CADA llamada. Escribir `textContent` es una mutación de DOM (reemplaza el nodo de texto) → dispara el `MutationObserver` de `enhance()` → que vuelve a llamar `updateBadge()` → **bucle infinito que congela la web** (`/carrito` colgaba 300s). Solo pasaba con el carrito **con items** (c>0); vacío (c=0) solo tocaba `style.display` (atributo, NO observado) → por eso "en cuanto guardas algo peta". Misma clase de bug que el bucle de `__acgOnRender` de acg-forms (v30).
- **Fix**: `updateBadge` idempotente — solo escribir `textContent`/`display` si el valor **cambia** (`if (c>0 && el.textContent!==txt)`, `if (el.style.display!==disp)`). acg-shop **?v=6** en las 66 páginas. Regla general: cualquier función llamada desde un MutationObserver que escriba en el DOM DEBE ser idempotente (comparar antes de escribir), o crea bucle.
- **Trampa de verificación**: medir lag con `setTimeout` en el navegador interno da ~900ms falsos porque la pestaña está en segundo plano (`visibilityState:'hidden'` → setTimeout throttleado a ≥1000ms, rAF pausado). NO es lag real del sitio. Verificar con `document.hidden` antes de creer un "lag".

### Subida de vídeo + recorrido/umbral editables (2026-07-18)
- **Vídeo en el admin**: el bucket `media` ahora admite `video/mp4|webm|quicktime|ogg` y **50 MB** (antes solo imágenes/PDF y 20 MB → de ahí el "mp4 no compatible" + "demasiado grande"). Nueva acción admin **`sign-upload`** = URL firmada de Supabase Storage → el cliente hace **PUT directo** (evita el tope ~4,5 MB de la función Netlify). `wixVidFile` usa esa vía (hasta 50 MB) con mensajes majos que sugieren YouTube para vídeos largos. Pegar un enlace de YouTube en el campo sigue siendo lo recomendado (la web pública ya lo convierte a embed). El admin NO tiene CSP → el PUT a supabase.co pasa.
- **Repeater del admin ahora soporta sub-campos `select`** (`repRow`/`repVal`, formato `['clave','Label','select',['op1','op2']]`).
- **Recorrido de propuestas con ESTADO**: el repeater `recorrido` (F, tab proposals) lleva `['estado','Estado','select',['hecho','en curso','pendiente']]` → verde (hecho) / azul (en curso) / gris (pendiente). La web pública (`Propuesta.dc.html`, sirve `/propuesta-ciudadana`) ya honra `it.recorrido` y ahora deriva `done/current` también de `estado`. La barra/meta usa los **umbrales reales** de la propuesta (`it.umbrales`) en vez de la escalera fija. Columnas `proposals.recorrido`/`umbrales` (jsonb) ya existían; `proposals.mjs` las devuelve.

### Posts de vídeo: separación y render (2026-07-18)
- **Los posts `tipo=video` salen SOLO en la sección de Vídeos**, no en la lista de noticias ni en el destacado (Actualidad `articles`/`featBase` filtran `catKey!=='video'`; Home `rNoticias` igual). La sección de Vídeos ya no mezcla demos (solo reales cuando `ready`).
- **Vídeo directo (mp4 subido) SE REPRODUCE**: `getPosts().videos` ahora da `src` (fichero) o `url` (YouTube); Actualidad usa `<video src>` para ficheros y thumbnail-link para YouTube; DetallePublicacion reproduce `<video>` (videoFile) o iframe (YouTube/Vimeo). **CSP**: añadido `media-src 'self' blob: data: https://*.supabase.co` y YouTube/Vimeo a `frame-src` (sin esto el `<video>` de supabase y los embeds de YouTube quedaban bloqueados por `default-src 'self'`).
- **Bug de guardado del vídeo (corregido)**: `wixCollect` lee `video_url` del span editable por `innerText`; si `wixRender` no repintaba ese span, se guardaba truncado (`.../uploads/` sin fichero). `wixVidFile` ahora fija `_ed.innerText=publicUrl` directamente antes y después de `wixRender`. El filtro de vídeos descarta URLs acabadas en `/` (rotas). DetallePublicacion muestra el vídeo aunque el post no sea `tipo=video` (basta `video_url`).
- **Para que un vídeo salga en la sección Vídeos, el post DEBE tener `tipo=video`** (el desplegable Tipo del admin). Si se sube a un post `noticia`, el vídeo se ve en el detalle del artículo pero no en la sección Vídeos.

### Vídeo en detalle: tarjeta + reproducción (2026-07-18)
- **Vídeo-fichero = tarjeta clicable** (miniatura+título) hacia `/actualidad/:slug`, no reproductor suelto (Actualidad `sc-if v.href`; `getPosts().videos` da `href`/`isFile`/`src`). En el detalle: miniatura de hero → cuerpo → **vídeo al final** (`sc-if post.videoFile`).
- **⚠ El dc-runtime BORRA los atributos booleanos pelados** (`controls`, `playsinline`, como con `disabled` — error #13b) → un `<video controls>` sale SIN controles (no deja darle play). Usar SIEMPRE la forma con valor: **`controls="controls"` `playsinline="playsinline"`** (así lo hace la sección de vídeos de Actualidad y funciona).
- **Imagen hero del detalle**: era `aspect-ratio:21/9` + `object-fit:cover` → recortaba fotos verticales. Ahora `object-fit:contain` + `max-height:540px` centrada sobre fondo navy → se adapta sin recortar (vale para vertical y horizontal).
- **Trampa de verificación (repetida)**: el navegador interno en segundo plano da `innerWidth:0` → imágenes/vídeo miden 0/2px (falso). Comprobar `document.hidden`/`visibilityState` antes de creer un "tamaño roto"; validar por `getComputedStyle` (objectFit) y `video.controls`, no por bounding boxes.

## Errores a no repetir

1. **Edit tool falla con Unicode U+2019** (`'` comilla curva). El valenciano lo usa mucho (`d'actuacions`). → Usar scripts Python para esas ediciones.
2. **NO usar ternarios en templates** (`{{ x ? a : b }}`). Solo `{{ var }}`. Lógica en `renderVals()`.
3. **renderVals() duplicados**: al editar data() y renderVals() a la vez, verificar que no se dupliquen variables.
4. **Write en archivo existente**: leerlo primero (aunque sea 5 líneas) o falla con "File has not been read yet".
5. **Home tiene nav/layout completamente diferente** al resto. No aplicar scripts batch sin excluirla.
6. **CSS header overrides** usan `header>div[style*='height:70px']` (subpáginas) vs `height:74px` (Home). No cambiar alturas.
7. **Scripts batch pueden comerse líneas estructurales.** El 2026-07-07 se encontraron 5 páginas variante (DetalleActuacion-bici/camaras/grao, FichaEvento-asamblea/barrio) con la línea `timeline:[` / `programa:[` eliminada por un batch anterior → SyntaxError → la página entera renderizaba vacía (a11y 71). Tras CUALQUIER batch, ejecutar el barrido de sintaxis extrayendo el script `data-dc-script` de los 67 archivos con `new Function()` (el barrido antiguo de "38 páginas" no cubría las variantes).
8. **CSP sin `unsafe-eval` rompe TODA la web en producción.** El dc-runtime evalúa las clases con `new Function()` → `script-src` DEBE incluir `'unsafe-eval'` (además de `'unsafe-inline'`). Descubierto en el primer deploy a Netlify (2026-07-07): las páginas renderizaban vacías solo en producción (en localhost no hay headers). Al migrar a Astro se podrán retirar ambos.
9. **La Asset Optimization de Netlify ("Pretty URLs") ROMPE los estilos inline.** Reescribe los `<a>` con comillas simples y escapa `'Public Sans'` como `'` (inválido en HTML) → los botones pierden padding/radius/font. Solución aplicada (definitiva, 2026-07-07): se eliminaron TODAS las comillas de los nombres de fuente (`'Public Sans'` → `Public Sans`, 2075 casos) — CSS lo permite y así el reescritor no tiene nada que escapar. Además `netlify.toml` con `skip_processing = true` (no bastó por sí solo con drag&drop) y recomendado desactivar Asset optimization en el panel. Las fuentes NUEVAS deben escribirse SIN comillas en los estilos.
10. **`onclick="form()"` en un `<button>` NO llama a `window.form`.** Los handlers inline resuelven primero las propiedades del elemento, y `button.form` es propiedad nativa (el form contenedor, null) → "form is not a function". En `<tr>` sí funciona (no tiene `.form`). Por eso "+ Nuevo" del admin nunca funcionó (2026-07-08). Regla: en handlers inline sobre botones, llamar siempre `window.miFuncion()` o evitar nombres que colisionen con propiedades DOM (form, name, id, value, type…).
11. **Las rutas relativas rompían TODAS las URLs bonitas anidadas** (`/actualidad/x`): `./support.js`, `assets/...` y `href="Pagina.dc.html"` se resolvían bajo `/actualidad/` → 404 o (peor) el catch-all `/actualidad/:slug` devolvía HTML como si fuera el JS → página de placeholders. Solución 2026-07-09: TODAS las referencias internas absolutizadas (`/support.js`, `/assets/...`, `/Pagina.dc.html`) en las 69 páginas. Las páginas NUEVAS deben usar siempre rutas absolutas.
12. **Al pegar código con regex vía heredoc bash→python, el escapado JSON puede comerse un nivel de backslash** (`\\n` llegó como `\n` real y rompió un regex JS). Tras cualquier parche por script, validar con `new Function()` el resultado (no solo el parseo del patch).
13b. **El dc-runtime NO expone el atributo `disabled` a selectores CSS** ni conserva nodos DOM ajenos al template en re-renders. El chat (acg-chat.js) fallaba porque buscaba `input[disabled][placeholder]` (nunca casaba) y porque insertaba una lista que el runtime borraba al re-renderizar. Solución 2026-07-09: detectar el input por su placeholder, lista persistente (mismo nodo JS re-adjuntado en un `enhance()` idempotente cada 1s), eventos cableados con guardas `data-acg-*`. Patrón general para mejorar DOM del runtime: bucle idempotente + marcadores, nunca asumir persistencia. Chat verificado end-to-end (usuario real vía admin API → POST/GET /api/chat OK).
13c. **Transparencia usaba datos demo hardcodeados que sobrescribían la API** (línea con `saldo:'+5.540 €'` machacaba `remote.saldo`). Ahora totales+saldo salen de /api/tesoreria cuando `movimientos>0`, con color/label dinámicos según signo (déficit rojo / superávit verde). `V()` NO existe en renderVals (solo en data()) — usar `es?a:b`.
13. **DetallePublicacion.dc.html es dinámica** (2026-07-09): carga el post por slug desde `/api/post-detail` en `_loadPost()` con fallback al contenido demo; catch-all `/actualidad/:slug` en `_redirects`. FichaEvento/DetalleCampaña siguen estáticas — misma técnica pendiente.
14b. **Heredoc bash→python corrompe acentos en Windows** (stdin cp1252): los `.replace()` con tildes no casan en silencio. Scripts de parcheo SIEMPRE a archivo `.py` UTF-8 (Write tool) y ejecutar el archivo.
14c. **El array `social:[...]` va al FINAL de data() sin coma** en la mayoría de páginas — los regex batch deben aceptar `]` con y sin coma (un batch solo casó 6/67 por esto).
14d. **Antes de "arreglar" una ruta/página, revisar la memoria del proyecto**: `/propuesta-ciudadana`→`Propuesta.dc.html` parecía un bug (nav de 3 ítems) pero era el detalle premium conectado esa misma mañana; el fix correcto era completarle el nav, no cambiar la ruta.
14e. **Lighthouse local (netlify dev) infla los tiempos** (sin gzip/CDN, funciones lentas): perf ~60 local = ~95 en producción. Optimizar con el reporte local (audits) pero medir en prod.
14f. **Template APP (admin.mjs): TODOS los backslashes del JS cliente van DOBLES** (`\.` `\/` `\n`) — un `/^\//` se cuece a `/^//` y revienta el script entero ("Unexpected token"). Validar SIEMPRE tras editar: extraer el <script> servido y pasarle `node --check` (patrón en scratchpad fix_regex_admin.py).
15. **Apóstrofes rectos en valenciano dentro de strings JS** (`d'Acció`, `conta'ns`) rompen el componente. Siempre U+2019 (`d’Acció`). Regex de reparación: `(\w)'(\w)` → `\1’\2` solo dentro del bloque script.

## Errores a no repetir (add 2026-07-18f) — el mapa "roto" era OTRA PÁGINA
- **Hay TRES páginas de detalle de propuesta**: `Propuesta.dc.html` (la que sirve la ruta pública **`/propuesta-ciudadana?id=`**, título "Detalle de propuesta"), `PropuestaCiudadana.dc.html` y `PropuestaDetalle.dc.html`. Se estuvo "arreglando" el mapa en PropuestaCiudadana mientras el usuario navegaba por /propuesta-ciudadana → Propuesta.dc.html, cuyo `_initPropMap` **ignoraba item.lat/lng** y pintaba un centroide fijo por barrio con fallback a Gandia centro → horas de fantasmas ("guardo la iglesia y sale el centro"). REGLA: antes de tocar una página, mirar en `_redirects` **qué archivo sirve la ruta que el usuario usa de verdad** y verificar por ESA URL; y cuando el usuario diga "yo veo X y tú ves Y", sospechar SIEMPRE que estáis en páginas/rutas distintas, no en cachés.
- Fix aplicado: `_initPropMap` de Propuesta.dc.html ahora prioriza `item.lat/lng` (zoom 16; barrio solo fallback) + auto-repintado si el runtime borra el DOM de Leaflet (13b) + `_headers` con `Cache-Control: max-age=0, must-revalidate` global para HTML (antes el HTML directo iba SIN cache-control → caché heurística de Chrome servía páginas viejas días).
- Guardado de ubicación blindado (WYSIWYG): admin y formulario guardan la posición REAL del marcador visible al guardar/enviar, no la del último clic.

## Errores a no repetir (add 2026-07-18b)
- **El bloque de "cifras/stats" NO se llama igual en todas las páginas.** Al hacer live el contador de miembros, apliqué el override sobre `d.heroStats` en Historia, pero Historia usa la clave **`cifras`** (Conócenos sí usa `heroStats`). El override sobre una clave inexistente se traga en silencio (`(d.heroStats||[]).map` → `[]`) y no hay error de sintaxis. REGLA: antes de sobreescribir una lista en renderVals, verificar el NOMBRE real de la clave en data() Y el `<sc-for list="{{ ... }}">` que la pinta en el template; no asumir que todas las páginas comparten nombre.
- **Verificación de deploy: NO curlear `/Pagina.dc.html`** — devuelve un stub de redirección de ~98 bytes (Netlify redirige a la URL bonita). Para comprobar el HTML servido, curlear la **URL bonita** (`/historia`, `/conocenos`…) con `-L`. El navegador sigue el redirect solo; curl sin `-L` da falsos "código no desplegado".

## Contador de miembros live (2026-07-18)
- Fuente de verdad = `/api/equipo` (`items.length`). Home (marquee) y Equipo (grid) ya reflejan altas/bajas. Conócenos: override de `heroStats` con `remote.items.length` (ya cargaba la API). Historia: NO cargaba la API → añadido `_loadTeam()` en componentDidMount + `state.teamN` + override de `cifras`. Si se añade el contador a otra página, mismo patrón (mirar si ya carga /api/equipo antes de duplicar el fetch).

## Errores a no repetir (add 2026-07-18)
- **La condición del runtime es `<sc-if value="{{ x }}">`, NO `cond=`.** Usé `cond=` en un `sc-if` nuevo de Equipo (foto vs iniciales) → nunca se cumpliría. Patrón correcto: dos `sc-if` hermanos con una var y su negada (`foto` / `noFoto`), igual que las tarjetas de la junta (línea ~156 de Equipo.dc.html). Antes de inventar atributos de plantilla, copiar el patrón que ya funciona en la misma página.

## Errores a no repetir (add 2026-07-18b) — gráfica de visitas a 0 en días recientes
- **PostgREST corta a 1000 filas por página y, SIN `order`, devuelve las MÁS ANTIGUAS.** El dashboard (`admin.mjs`, acción `analytics`) leía `analytics_events ... limit=10000` sin order y bucketizaba en JS → con >1000 eventos, los días recientes (hoy/ayer) se quedaban fuera → `serie30` y `horas8` salían a 0 justo en hoy/ayer, mientras las tarjetas (que usan `count`, no filas) sí acertaban. Arreglo: helper `rowsPaged(baseQ)` que pagina con `&order=created_at.desc&limit=1000&offset=N` hasta agotar. Aplicado a serie30q, horaRows e inRows. REGLA: cualquier lectura de filas para agregación temporal DEBE paginar y ordenar; nunca fiarse de `limit=N` alto (hay tope de 1000).

## WhatsApp real + popup mapa de la sede (2026-07-18j)
- **WhatsApp**: el mock `wa.me/34600000000` (solo estaba en Contacto) → real **`wa.me/34637656279`** (mismo nº que el `tel:`). Verificado que no queda ningún `600000000` en el repo. (El `612345678` de Afiliarse es solo el ejemplo del mensaje de validación, NO un mock.)
- **Popup mapa sede** (`acg-forms.js`, global): `window.acgSedeMapa()` + delegación de clic en cualquier `a[href$="#sede-mapa"]` → modal cerrable (X / botón / clic fuera / Esc) con **iframe de OpenStreetMap** (marcador en 38.9757828,-0.1832722) + botón **"Ver dónde está"** → **Street View** de Google (`maps/@?api=1&map_action=pano&viewpoint=LAT,LNG`). acg-forms **?v=33**. Sede = Avda. República Argentina 42, entresuelo · Local 3 · 46702 Gandia. Wired: Contacto (tarjeta "Ver en mapa", href '#'→'#sede-mapa') y Participa (enlace nuevo `t.verMapa`). **CSP**: `frame-src` += `https://www.openstreetmap.org`. acg-forms **?v=32**. Botones cabecera móvil: el usuario confirma que los quiere en **solo icono**.

## Cabecera: reactividad idioma + overflow botones (2026-07-18i)
- **Bug idioma**: los botones inyectados por `acg-forms.js` (`#acg-unete-btn`, `#acg-auth-btn`) capturaban `VA` una vez al cargar y hacían `return` si ya existían → NO cambiaban al togglear ES/VA (solo tras recargar). Fix: `curVA()` lee el idioma en vivo + `ponBoton/ponUnete` son find-or-create y **refrescan la etiqueta** en cada `__acgOnRender`. ⚠ CLAVE: guardar `data-txt` y **solo reescribir innerHTML si el texto cambió** — si no, cada reescritura dispara el MutationObserver de `__acgOnRender` → **bucle infinito que CONGELA el render** (me pasó en v30). Verificado: ES↔VA cambia botones al instante, sin recargar, sin freeze.
- **Overflow / Uneix-te empuja el carrito**: `<style id="acg-hbtn-css">` (inyectado 1 vez): host `margin-left:auto` con `flex-wrap:nowrap;min-width:0`; a **≤1100px** los botones Únete/Cuenta pasan a **solo-icono** (`.acg-hbtn-txt{display:none}`), ahorra ~126px, el carrito no se desplaza. Texto envuelto en `<span class="acg-hbtn-txt">`. acg-forms **?v=31**.
- ⚠ PENDIENTE VERIFICAR EN MÓVIL REAL: el Chrome remoto de pruebas está fijo a 1920px y el navegador interno bloqueó la navegación → no pude emular un teléfono. La lógica está puesta; conviene que el usuario confirme en su móvil y reporte breakpoints concretos si algo aún desborda.

## Crear propuesta simplificado (2026-07-18h)
- Quitados del form (`CrearPropuesta.dc.html`): campos **nombre**, **correo** y **desplegable de barrio** (y el checkbox de novedades `.acg-mkt-check`, que `acg-forms.js` ahora inyecta SOLO en Contacto — condición `#privacy-check`, no `#anon-check`). Step 3 pasa a "Privacidad" (solo check de anónimo + aviso "tu nombre y correo se toman de tu cuenta").
- **Nombre/correo del backend**: `propuesta.mjs` ya usaba `perfilNombre` para contacto_nombre; ahora `contacto_email = user.email` (sesión), no el form.
- **Barrio autodetectado**: al fijar punto en el mapa (clic/arrastre/buscador), `acg-forms.js` hace **reverse geocoding Nominatim** (`/reverse`) y matchea el barrio con `BKW` (regex→etiqueta canónica). Clave: iterar los campos del address **del más específico al más general** (quarter→neighbourhood→suburb…) para que "Playa de Gandía" (quarter) gane a "Grao y Playa" (suburb) — sin esto, Playa detectaba Grau. Envía `barrio: window._acgPropBarrio || 'Toda la ciudad'`. Verificado: Beniopa/Centro/Grau/Playa OK. acg-forms **?v=29**. CSP connect-src ya permite nominatim.
- ⚠ La detección depende de los barrios de Nominatim (no 1:1 con los 11 del partido); es best-effort, pero el lat/lng exacto siempre se guarda.

## Fix autor de propuestas (2026-07-18g)
- Bug: al crear propuesta con login, el form no capturaba "nombre" → `contacto_nombre` quedaba null aunque NO marcaras anónimo → `proposals.mjs` (`tarjeta()`, línea ~39: `contacto_nombre || 'Anónimo'`) mostraba **todo como "Anónimo"**. Afecta lista y detalle (ambos usan `tarjeta()`).
- Fix en `propuesta.mjs`: se lee `profiles.nombre` de la cuenta (junto al check de `bloqueado`) y `contacto_nombre = anonimo ? null : (perfilNombre || nombre)`. Así, no-anónimo → nombre de la cuenta; anónimo → null → "Anónimo". Sin migración. Existentes afectadas eran solo basura de prueba/seed → no se hizo backfill.
- **Ficha del autor en el detalle** (`Propuesta.dc.html`, la que sirve `/propuesta-ciudadana`): mostraba `autorMeta` fijo "Vecino/a de Gandia" y `autorProps/autorApoyos` a "—". Fix: `proposals.mjs` (path `?id=`) añade `user_id` al select y, **solo si NO es anónimo** (contacto_nombre presente, para no desanonimizar), devuelve `autorTitulo` (rango vía `titulos.mjs`, con service role porque profiles no es legible por anon), `autorTitulos` (insignias), `autorProps` (nº propuestas publicadas del autor) y `autorApoyos` (suma de a_favor). El front pinta rango real + contadores. Verificado: Nico → "Colaborador/a", 4 propuestas.
- **Foto del autor** (2026-07-18g bis): mismo bloque devuelve `autorAvatar` (profiles.avatar_url, '' si 'none'); `Propuesta.dc.html` pinta `<img>` (sc-if value/autorNoAvatar) en los DOS avatares del autor (línea superior + tarjeta), o iniciales si no hay foto. CSP img-src ya permite *.supabase.co. Solo no-anónimo. **También en las TARJETAS de la lista de Participación** (`Participacion.dc.html` línea ~247): la ruta lista de `proposals.mjs` hace un lookup batch `profiles?id=in.(...)&select=id,avatar_url` (service role) y adjunta `autorAvatar` a cada item; el front pinta img/iniciales igual (sc-if autorAvatar/autorNoAvatar). Verificado: Nico con foto, Anónimo con iniciales.

## Avisos a Telegram (2026-07-18f)
- **Bot propio** `@Avisos_accioncivil_bot` (chat_id del usuario: 6193719330). Relay `netlify/functions/alerta-telegram.mjs` (`/api/alerta-telegram`) reenvía a Telegram; env en Netlify: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHAT_ID`, `TELEGRAM_WEBHOOK_SECRET` (secreto en scratchpad `tg_secret.txt`). Test: `GET /api/alerta-telegram?test=1`. Filtra: solo reenvía Sentry con `action=created` (evita ruido de resolved/assigned).
- **Errores (Sentry) → Telegram**: integración interna de Sentry "Avisos Telegram" (org white-lab, proyecto javascript) con webhook a `/api/alerta-telegram?source=sentry&s=<secret>`, permiso Issue&Event=Read, suscrito a **Issues→Created**. ✅ Un error nuevo dispara aviso.
- **Caídas (uptime) → Telegram vía HetrixTools** (NO UptimeRobot: su free tiene Telegram Y webhook de pago; además el relay vive en Netlify → inútil si Netlify cae, por eso el uptime necesita un monitor externo con Telegram propio). HetrixTools free, cuenta del usuario (nicosfb2016@gmail.com), monitor "Web Acción Civil" (Website, Amsterdam/London/Frankfurt) → Contact List "Default Contact" con Telegram chat_id 6193719330 (bot oficial **@HetrixTools_bot**, el usuario le dio START). ⚠⚠ GOTCHA: mientras la cuenta HetrixTools estaba **sin verificar el email** ("limited functionality"), guardar el Telegram en el Contact List **NO persistía** (el "Send test notification" en caliente sí funcionaba y engañaba) → una caída real NO habría avisado. Tras verificar el email hubo que **re-guardar** el chat_id (fila muestra la tilde azul de Telegram = OK). VERIFICADO end-to-end: caída simulada (keyword inexistente `CAIDA_PRUEBA_ZZZ999` → down → alerta Telegram → quitar keyword → recovered) + "Notification Sent." de test. Email ya verificado.
- UptimeRobot free queda como respaldo (aviso por email, ya activo).

## Monitorización externa (2026-07-18e) — CONFIGURADO Y VERIFICADO
- **UptimeRobot** (cuenta del usuario): monitor HTTP a `accioncivilgandia.netlify.app` cada 5 min, aviso por email por defecto (Telegram/push lo añade el usuario). Sin código.
- **Sentry** (org `white-lab`, proyecto Browser JavaScript, solo Error Monitoring): loader oficial `https://js-de.sentry-cdn.com/f00a0f84bd56e8f63c9f648b284bd50d.min.js` inyectado al PRINCIPIO de **`acg-forms.js`** (cubre todas las páginas sin tocar support.js; guarda `location.hostname==='localhost'` → no monitoriza en dev). **CSP en `_headers`** ampliado: script-src += `https://*.sentry-cdn.com`, connect-src += `https://*.sentry.io`. `acg-forms.js` subido a **?v=26** en todas las páginas. Verificado: error de prueba llega al feed de Sentry, sin violaciones de CSP en consola.
- Plan Sentry: trial 14 días (volumen ilimitado), luego free (5k errores/mes, sin tarjeta).
- **Pendiente (del usuario / cuando haya dominio propio)**: Google Search Console (requiere verificar dominio → aplazado hasta enlazar el dominio); Telegram en UptimeRobot para aviso instantáneo.

## Sistema de títulos/rangos ligado a cuentas (2026-07-18c) — APLICADO Y DESPLEGADO
- **Migración 020**: `profiles` += `es_donante`,`es_afiliado`,`voluntariado`; `voluntarios` += `user_id`,`tipo`(voluntario|colaborador). Aplicada vía Chrome SQL editor.
- **Rangos (helper `lib/titulos.mjs`)**: línea jerárquica **ciudadano<voluntario<colaborador<afiliado** (se muestra SOLO el más alto) **+ donante** aparte → **máx. 2 insignias**. `TITULO_META` (emoji/color/ES-VA), `rangoDe()`, `titulosDe()`, `titulosPayload(p,lang)`.
- **Ciudadano SIN logo** (2026-07-18d): `TITULO_META.ciudadano.emoji=''` y `titulosDe(p, incluirBase)` omite ciudadano salvo `incluirBase=true`. chat/comentarios NO muestran nada para un ciudadano normal; `cuenta.mjs` pasa `incluirBase=true` para que el área muestre "Ciudadano/a" como texto. Afiliado=⭐ (revisable).
- **Afiliado = Stripe por email, NO la tabla `afiliados` antigua** (decisión del usuario: solo los afiliados reales de Stripe llevan rango). `sync-titulos` lista suscripciones activas de Stripe (`subscriptions?status=all&expand[]=data.customer`, excluye canceled/incomplete) → casa `customer.email` con cuentas → `es_afiliado`. `cuenta.mjs` usa el flag guardado (NO recomputa afiliado en vivo: hacerlo contra `members` vacía borraba el flag). Caso real: 2 afiliaciones activas comparten el correo `a.alcazar.h@gmail.com` que NO es cuenta web → 0 insignias hasta que esa persona entre con ese correo (activación por `invoice.paid` mensual o `sync-titulos`). Modo elegido: automático por email (sin asignación manual).
- **Enlace por email**: `es_donante` = donación pagada con el email de la cuenta; `es_afiliado` = suscripción Stripe activa cuyo email coincide con la cuenta. Se mantienen: (a) webhook (`stripe-webhook.mjs` → `marcarTituloPorEmail` en donación pagada e invoice.paid), (b) `cuenta.mjs` refresca en vivo los flags propios, (c) acción admin **`sync-titulos`** (backfill/repair: recalcula todos por email; exención añadida al guard `bad_table`).
- **Voluntarios ahora por CUENTA**: admin → Voluntarios pide **email** (debe estar registrado); `guardarVoluntario()` (intercepta save/del de `voluntarios`) resuelve email→user_id (auth admin API `?email=`), **snapshot** de nombre+avatar en la fila (Equipo lee con anon, sin acceso a profiles), y marca `profiles.voluntariado`. Al borrar/desactivar, limpia el título si no le quedan otros.
- **Insignias en**: chat (`acg-chat.js` v8, emoji junto al nombre, `/api/chat?lang=`), área ciudadana (`cuenta.mjs` sección "Mis títulos", pills), comentarios (`PropuestaCiudadana.dc.html` `{{ c.badges }}` + `/api/comentarios?lang=`). `chat/cuenta/comentarios` seleccionan `profiles(...,es_donante,es_afiliado,voluntariado)` y devuelven `titulos[]`.
- **Verificado en prod**: Nico (cuenta real que donó) asignado voluntario por email → sale en Equipo con **foto de su cuenta** + `voluntariado=voluntario`+`es_donante=true` (→ 🙋+💛). chat 401 / comentarios 200 / cuenta 401 (sin 500 por columnas). ⚠ Nico y "Laura Giner (prueba)" son PRUEBAS: borrar en admin → Voluntarios cuando el usuario quiera (Nico = su propia cuenta, sale pública como voluntario).
- ⚠ **Orden de despliegue**: migración 020 ANTES del deploy (las nuevas queries seleccionan esas columnas; sin ellas → 400).

## Voluntarios + barrios unificados (2026-07-18)
- **Lista canónica ÚNICA de barrios** = tabla `barrios` (migración 019 la lleva a los 13 de Crear propuesta: renombra 7 por slug preservando IDs/FKs + inserta raval/germanies/republica-argentina/playa que faltaban → esas propuestas antes se guardaban sin barrio). Columna nueva `orden`. TODOS los desplegables leen de ahí: admin (campo tipo `barrio`, ya lo hacía), Crear propuesta (ahora `fetch /api/barrios` con sentinelas 'Toda la ciudad'/'Otro barrio' solo en ese form), y el nuevo `barrio_id` de voluntarios. Fallback `seedBarrios` en content-seeds.mjs igualado.
- **Nueva tabla `voluntarios`** (nombre, barrio_id, foto, orden, activo) + pestaña admin 🙋 "Voluntarios" (CRUD config-driven, foto drag&drop igual que equipo). `/api/equipo` ahora devuelve `voluntarios[]` (join a barrios). Equipo → "Red de barrios": foto real o iniciales, contenedor con `max-height`+scroll, y si el feed llega vacío la sección se **oculta** (fuera los 12 falsos). Endpoint público nuevo `/api/barrios`.
- ✅ **APLICADO Y DESPLEGADO (2026-07-18)**: migración 019 ejecutada en SQL Editor (vía Chrome del usuario, Monaco setValue+Run). OJO: el INSERT multi-fila dejó los 4 barrios nuevos con `orden` = default 100 en vez de 20/30/40/100 — se corrigió por PATCH REST (raval=20, germanies=30, republica-argentina=40). Deploy a producción con `netlify deploy --prod --dir=deploy_netlify --functions=deploy_netlify/netlify/functions` (CLI ya autenticado como el usuario). Voluntario de prueba "Laura Giner (prueba)" (barrio raval) creado por REST y verificado en /equipo. Los 12 falsos fuera. `/api/barrios` y voluntarios en `/api/equipo` OK en prod.
- **Trampa de diagnóstico**: `/Equipo.dc.html` hace **301 → /equipo** (pretty URL). `curl -s` sin `-L` devuelve el body del 301 (98 bytes) → grep da 0 y parece "no desplegado". Verificar SIEMPRE contra la URL bonita con `curl -sL .../equipo`, no contra el `.dc.html`.

## Botones del nav (dcha.) uniformes + errores de campo en Crear propuesta (2026-07-18k)
- **Tamaño uniforme 38px** para TODO el lado derecho del nav (PC y móvil) vía `acg-hbtn-css` en acg-forms.js: toggle ES/VA (botones 32px + envoltorio 3px = 38), CTA Participa y buscador Home (38 alto, icono 38×38 en ≤1439px), Únete/Cuenta (38 alto; **solo-icono 38×38 en ≤1300px** — antes 1100: a 1280px el carrito se RECORTABA, overflow preexistente) y gap del host a 8px en ≤1300px. Los selectores largos empatan en especificidad con los `!important` de cada página y ganan por venir después en el `<head>`. acg-forms **?v=47** (65 páginas).
- **Errores de campo con scroll directo** (form propuesta): validación en cliente = reglas backend (título 5-140, descripción 30-5000, categoría elegida) con `marcaError()` — borde rojo + motivo debajo (`role=alert`) + `scrollIntoView` al primero + focus; se limpia al escribir. `post()` acepta 7º param `onErr` y el branch propuesta mapea `invalid_titulo/descripcion/categoria` del backend a su casilla. Patrón reutilizable para otros forms (`limpiaErrores`/`marcaError`).
- **Dominio propio ACTIVO**: el deploy de esta sesión salió ya con Production URL `https://accioncivilgandia.es` (web nueva verificada en el dominio). Pendiente la cascada post-dominio (canonicals, sitemap, 301 desde *.netlify.app, APP_BASE_URL, monitores) — ver memoria pendientes-acg.

## Cursor de texto fuera del contenido no editable (2026-07-19)
- El I-beam ("cursor de escribir") del navegador sobre texto normal confundía al usuario. Reglas globales en `acg-hbtn-css` (acg-forms.js): `body{cursor:default;caret-color:transparent}` + `input,textarea{cursor:text;caret-color:auto}` + pointer en `a[href],button,select,checks,summary`. Barrido Playwright de las 66 páginas (ES/VA, 1280 y 375px): 0 textos con cursor de texto; campos reales conservan cursor y caret. El `not-allowed` del chat sin sesión es intencional. acg-forms **?v=50**.
- **`Afiliarse.dc.html` es una página SUELTA** (HTML plano de la otra sesión, sin dc-runtime NI acg-forms.js — a propósito: acg-forms le colaría el popup de newsletter en pleno pago Stripe). Las mejoras globales de acg-forms NO le llegan: hay que replicarle a mano lo que aplique (se le copiaron las reglas de cursor en su `<style>`). Si nace otra página suelta, misma consideración.
- ⚠ Las versiones **?v=48 y ?v=49 las subió OTRA sesión** (2026-07-18 ~23:58, sin nota en este archivo) que también desplegó y creó Afiliarse. Antes de bump de versión, mirar la actual con grep (no asumir la última anotada).

## Fin del "flash de demo" al cargar (2026-07-19b) — causa raíz sistémica
- **CAUSA RAÍZ**: 20 páginas asignaban la clase esqueleto `acg-sk` en renderVals pero NO definían su CSS (solo MapaActuaciones lo tenía) → el esqueleto era inerte y la demo se veía tal cual durante la carga. FIX: definición ÚNICA de `.acg-sk` (+shimmer) en el CSS global que inyecta **acg-forms.js** (`acg-hbtn-css`) — llega a las 65 páginas antes del primer render. Ya NO hace falta copiarla por página (las copias locales de Mapa/Agenda/Propuesta son inofensivas). acg-forms **?v=51**.
- Fixes específicos además del CSS: **Propuesta.dc.html** headMeta con rama de carga (metadatos '…', nunca la fecha demo «3 jun 2026» — era la queja del usuario); **Agenda** calendario VACÍO mientras carga (`calEvents = ready ? remoto : {}` — las celdas no tienen esqueleto por-celda, así que nada de actos demo); **Accion** stats con skCls + `class="{{ s.skCls }}"` en el tile; **Campanas** `finDisp` oculto también durante la carga (`!ready || !finished.length` — el header "RESULTADOS/Conseguidas" aparecía y luego se escondía al venir vacío).
- **Método de auditoría que funciona**: Playwright contra PRODUCCIÓN con `page.route('**/api/**')` retrasando 4-5s las respuestas → capturar `innerText`/screenshot durante la ventana de carga y comparar con el final. OJO: (1) los handlers de route PERSISTEN entre llamadas a run_code — hacer `unroute` al empezar; (2) en el sandbox de run_code NO existen setTimeout/require — usar `page.waitForTimeout`; (3) el diff de innerText da FALSOS positivos por capturas a mitad de re-render (headers estáticos "desaparecidos") — confirmar cada sospecha con sonda dirigida antes de tocar nada.
- Regla ya vigente reforzada: sección cuya visibilidad depende de si lo remoto viene vacío → `display:none` TAMBIÉN mientras carga (pop-in si existe > enseñar-y-esconder).

## Títulos: Vecino/a por defecto + Presidente 👑 (2026-07-19h)
- **`lib/titulos.mjs` reescrito**: rango base `ciudadano`→**`vecino`** (🏘️ Vecino/a · Veí/na) y AHORA SE MUESTRA SIEMPRE (antes ciudadano iba sin insignia y oculto — decisión revertida a petición del usuario: toda persona registrada es "Vecino/a" por defecto). Nuevo rango tope **`presidente`** (👑) por encima de afiliado. Jerarquía: vecino<voluntario<colaborador<afiliado<presidente (+donante bonus, máx 2 insignias). Alias `TITULO_META.ciudadano=vecino` para retrocompat.
- **Presidente = ÚNICA cuenta con honorífico "Presidente" en el nombre** (SPECIAL_ACCOUNTS `accioncivilgandia@gmail.com`, `names.mjs`). `esPresidente(p)` = `/^presiden/` sobre `splitCargo(p.nombre).cargo` → "Presidente" sí, "Vicepresidente" NO. Los usuarios normales no pueden autoasignarse cargo (cleanName bloquea CARGO_RE) → detección segura sin columna nueva ni migración.
- **CLAVE — pasar `nombre` a titulosPayload**: chat/comentarios ya seleccionaban `nombre`; se AÑADIÓ a `cuenta.mjs` (línea ~129) y al select de autor en `proposals.mjs` (si no, el presi no lucía corona en su área ni como autor). Verificado con test node: presi→👑, vecino→🏘️, vicepresi→🏘️ (sin corona).
- Emoji del nav Registros: `🧑‍💻` (ZWJ, se renderiza en 2 glifos y tapaba la palabra en el móvil) → **`👤`** (glifo único). Regla: en el menú del admin usar SIEMPRE emojis de un solo codepoint.
- Registros web ahora pinta los `titulos` reales (corona/vecino/afiliado/donante) en vez de flags ad-hoc.

## Admin: apartado "Registros web" (cuentas creadas) — 2026-07-19g
- Nueva pestaña **🧑‍💻 Registros web** (tras Suscriptores en `ORDEN`): tabla de personas que han creado cuenta (Supabase Auth), con foto/iniciales, email, alta (Madrid), vía (Google/Email) y títulos (afiliado/voluntario/donante/bloqueado). Acción backend **`registros-list`** (auth admin API paginada + join a `profiles`) + `renderRegistros()` + `registrosCsv()` (export). Total real + "+N hoy" + nota de cuántas cuentas de sistema se ocultan.
- **Cuentas de sistema/prueba OCULTAS** (no son personas): helper `esSistema()` filtra `@seed.acg`, `@acgtest.local`, **`@acg-test.local`**, `@acg.test`, `sistema-oficial@`, `moderacion-test@`, `test_*`. El mismo criterio se aplicó a `usuariosReales()` del dashboard → **el contador de "miembros" pasa de 5 a 4** (antes contaba `test_…@acg-test.local` porque el filtro solo tenía `@acgtest.local` SIN guion — bug del guion). La cuenta de prueba NO se borra (el usuario la deja); solo se oculta del recuento.
- Estado real 19-jul: **4 registros reales** (Nico=usuario, David, cuenta oficial del partido, Alex Gregori) + 1 cuenta de prueba oculta.
- ⚠ Recordado error #14f: los `\n` del `registrosCsv` cliente van DOBLES (`\\n`) porque el script va dentro del template APP; sin doblar rompe el <script> servido. Validar siempre con `node --check` sobre el <script> extraído por curl.

## Footer móvil compacto: Explorar+Legal en 2 columnas (2026-07-19f)
- Otra sesión lo hizo en las 61 subpáginas (`footerGrid: isMob?'1fr 1fr'` + `footerSpan` en marca/boletín) pero **la Home móvil tiene footer HARDCODEADO aparte** (error #5 de siempre: la Home va distinta) → seguía en una columna y era donde miraba el usuario. Arreglado: en index.html (bloque móvil ~línea 910) Explorar y Legal van ahora dentro de un grid `1fr 1fr`. El usuario reportó "lo pedí a otro chat y no lo veo" → era esto + posiblemente sin deploy (mis deploys lo publicaron todo). Verificado en prod a 375px: Home y subpáginas iguales.
- ⚠ La otra sesión añadió **banner de cookies** y GA4 — al verificar en móvil con Playwright, cerrar popup newsletter + banner antes de medir/capturar.

## Legal aplicado + autoalojado total + 301 SEO (2026-07-19e)
- **Privacidad ampliada** (vía script Python por U+2019): encargados ahora incluyen **Stripe, Sentry, Cloudflare, Nominatim/Photon** + Marco de Privacidad UE-EEUU + **edad mínima 14 años** (art. 7 LOPDGDD). De paso se repararon **2 chars de control corruptos** (\x01\x02) en el texto VA (restos del bug re.sub 2026-07-13).
- **TODO autoalojado (RGPD: ni una IP a CDNs)**: Google Fonts → `/assets/fonts.css` + 14 woff2 en `/assets/fonts/` (66 páginas reescritas, script `fonts_selfhost.py`); Leaflet 1.9.4 → `/assets/leaflet/` (13 páginas); **React/ReactDOM/Babel → `/assets/vendor/`** (URLs cambiadas DENTRO de support.js — única edición permitida: solo strings de CDN). CSP: `font-src` += `'self'`. ⚠ `/assets/*` es immutable 1 año: si algún día cambia `fonts.css`, renombrar o añadir ?v=. `/support.js` ahora con regla de caché propia (Cloudflare la eleva a 4h).
- **YouTube → youtube-nocookie.com** en el embed de DetallePublicacion (CSP ya lo permitía). "Normas de la comunidad" ya existían en Comunidad ("Las normas de la casa").
- **SEO**: 301 de las URLs de la web VIEJA indexadas (`/nuestra-gente`→/equipo, `/acciones-poilticas-desde-nuestra-fundacion`→/historia, `/es/*`→/:splat) + **`*.netlify.app/* → accioncivilgandia.es 301!`** en _redirects + `APP_BASE_URL` en Netlify env. La cascada de canonicals/sitemap/robots la hizo OTRA sesión en paralelo (ya verificado: 62 canonicals al dominio). Todo verificado en prod con curl (301s correctos) y Playwright (fuentes locales cargan, mapas funcionan, textos legales visibles).

## Home: barra de progreso de tendencias conectada (2026-07-19d)
- La tarjeta de la Home decía "Meta: 100 apoyos" sobre la barra → `pintaApoyoUI` (que solo reconoce encabezados "X / Y") NO actualizaba la barra al votar. Ahora `tp.umbral` = **"X / Y apoyos"** (mismo formato que Participación y el detalle) → barra y número se actualizan en vivo al votar también en la Home (escritorio y móvil comparten el binding). Fuente única: /api/proposals + /api/votos.
- **Auditoría legal completa** en `docs/AUDITORIA_LEGAL_2026-07-19.md` (estado ✅ + 7 mejoras priorizadas: añadir Stripe/Sentry/Cloudflare a Privacidad, autoalojar Google Fonts, edad mínima 14, youtube-nocookie, autoalojar Leaflet, citar Nominatim/Photon, normas de comunidad).
- **SEO 2026-07-19**: marca #2; "partido independiente Gandia elecciones 2027" #7; genéricas ("partido político Gandia", "propuestas ciudadanas Gandia", "participación ciudadana Gandia") fuera del top-10. OJO: Google aún indexa URLs de la web VIEJA bajo el dominio (p. ej. /nuestra-gente, ahora 404) → añadir 301 de rutas antiguas en `_redirects` cuando se haga la cascada post-dominio.

## Errores a no repetir (add 2026-07-19c) — el botón "Apoyada" mostraba la META (100) tras recargar
- `estadoListaInicial` (acg-forms) rascaba el nº de apoyos del DOM: "elemento anterior a la barra de progreso, primer número". En la Home ese elemento es **`tp.umbral` = "Meta: 100 apoyos"** → tras recargar, el botón pintaba "Apoyada (100)" (el usuario: "sube hasta 100 likes si actualizo"). Fix: fuera el scraping — el contador se pide a **`/api/votos?proposalId=`** (helper `pideCnt`, 1 petición cacheada por propuesta apoyada; mientras llega, el botón va sin número — `pintaBtnVotado` ya no pinta "()" vacío). acg-forms **?v=53**. REGLA: NUNCA leer contadores rascando texto del DOM (cada página ordena la tarjeta distinto); siempre de la API.

## Errores a no repetir (add 2026-07-19b) — el voto se cablea por TEXTO del botón; el VA no casaba
- El voto delegado de acg-forms.js reconoce los botones por su texto (`/^(apoyar|recolzar|apoyada|recolzada)/`), pero los botones reales en valenciano dicen **«Donar suport»** (Home) y **«Suport»** (Participación) → en VA el clic no hacía NADA (el voto nunca llegaba a la BD; en ES sí funcionaba). Fix: añadido `donar suport|dóna suport|suport` a las TRES regex (delegación del clic, marca de "ya votado" en detalle y en listas). acg-forms **?v=52**. REGLA: cualquier handler que matchee por texto visible debe cubrir los DOS idiomas — al crear un botón nuevo con texto V(es,va), grep de las regex de acg-forms que puedan matchearlo. Verificación sin cuenta: sin sesión, el clic debe abrir el popup de login (si no lo abre, el botón no está cableado).

## Errores a no repetir (add 2026-07-19) — colisión de atributos data-* en el admin
- El admin tiene **listeners delegados GLOBALES en `document`** que capturan por atributo en TODA la página: `[data-pdel]` (borrar propuesta), `[data-parch]`, `[data-cdel]`, `[data-rrev]`, `[data-af*]`, `[data-go]`, `[data-hide]`, `[data-block]`… El botón de eliminar pedido usó `data-pdel` → cada clic disparaba TAMBIÉN `propDel` (borrado de propuestas) y "saltaba a propuesta ciudadana". REGLA: antes de inventar un `data-*` nuevo en el admin, `grep "closest('\[data-"` y elegir un nombre que no exista (se usó `data-peddel`).

## Admin Tienda: borrar pedidos erróneos (2026-07-19c)
- Acción **`pedido-eliminar`** en admin.mjs: exige `motivo` y que el pedido esté **`cancelado`** (si no → error "Cancélalo primero"); DELETE de `orders` (order_items cae por FK cascade); todo queda en `audit_log` (motivo, número, email, total). Cliente: botón "🗑 Eliminar pedido" SOLO en pedidos cancelados (fila del selector de estado) → `askPrompt` motivo + `askConfirm` danger (helpers propios, nunca nativos). Verificado: script cliente servido en prod parsea (`node --check` sobre el <script> extraído con curl). El flujo con datos reales no se pudo probar end-to-end (requiere login admin).

## Errores a no repetir (add 2026-07-13)
- **re.sub replacement con `\2` en string NO raw** → Python lo interpreta como escape octal `chr(2)` (STX invisible), NO como referencia de grupo. Rompió setState en Conocenos/Equipo ("Invalid or unexpected token"). REGLA: al concatenar clave dinámica en un replacement de re.sub, usar `\g<1>`/`\g<2>` dentro de raw strings (`r'\g<1>'`), nunca `\1`/`\2` en strings normales.

## Donaciones por mes + liquidación en tesorería (2026-07-13)
- Webhook `payout.paid` (stripe-webhook.mjs): al ingresar Stripe (día 1), suma donations pagadas no liquidadas + payments (cuotas) no liquidados, crea 2 apuntes en `tesoreria` (categorías 'Donaciones' y 'Cuotas'), marca `liquidada_at`. Requiere columnas `donations.liquidada_at` y `payments.liquidada_at` (migración pendiente de ejecutar por el usuario).
- Webhook Stripe (we_1TsW5t...) enabled_events ampliado: +charge.refunded, +charge.dispute.created, +payout.paid.
- Admin: card 'Este mes (pendiente de ingreso)' (donPend = pagadas con liquidada_at NULL) + card histórico total + lista 'Últimas donaciones' (nombre/fecha/importe/PENDIENTE) + col Fecha e Ingresada en tabla donations.
- Afiliaciones: afiliacion.mjs mode:subscription + recurring month → cobro mensual recurrente OK. invoice.paid consolida cada mes en members + registra en payments.

## Admin Afiliados conectado a Stripe (2026-07-13)
- Sección 'afiliados' del admin ya NO es CRUD manual: renderAfiliados() (client) llama a action 'afiliados-stripe' (backend lista subscriptions de Stripe con expand customer). Muestra: nombre (metadata), email, cuota €/mes, próximo cobro (current_period_end), estado (active/pausado/baja_fin/past_due), + resumen (socios activos, acumulado cuotas del mes pendiente de ingreso desde payments no liquidados, aviso ✓ingresado). Botón "Copiar enlace de afiliación" (→ /participa). Acciones por socio: afiliado-pausar (pause_collection void), afiliado-reactivar (clear pause + cancel_at_period_end false), afiliado-baja (cancel_at_period_end=true → baja a fin de periodo). Botones data-af + listener delegado en document.
- 'members_inbox' ('Afiliaciones') eliminado del menú ORDEN. 'donations' movido justo tras 'afiliados'.
- NO altas manuales: quien quiera afiliarse usa el enlace /participa (form → checkout Stripe).

## Errores a no repetir (add 2026-07-16) — demo colándose tras el esqueleto
- El "flash de demo" NO era caché: eran fallbacks `remote.X.length ? remote.X : d.demoX` que mostraban demo PERMANENTE cuando la API respondía con lista vacía (Campañas finalizadas=0, Actualidad videos=0), secciones demo sin API (comunicados) y bindings demo sin skeleton (saldo tesorería, compStats). REGLA: (1) con `remoteStatus==='ready'` usar SIEMPRE lo remoto aunque venga vacío y ocultar la sección (`display:none`), la demo solo puede existir BAJO esqueleto; (2) TODO binding alimentado por fallback demo debe llevar clase skeleton; (3) al auditar un flash, comprobar la visibilidad computada (el esqueleto usa visibility:hidden → innerText/grep dan falsos limpios).

## Errores a no repetir (add 2026-07-16b) — UI inyectada tarde con setInterval
- El "pop-in cutre" del botón Mi cuenta / carrito / Tienda: acg-forms.js y acg-shop.js inyectaban con `setInterval(fn, 1000-1500)` → la página pintaba y la UI llegaba hasta 1,5 s después. REGLA: toda inyección de UI post-render usa `window.__acgOnRender(fn)` (MutationObserver → corre en el mismo frame que el render del runtime, antes del pintado; interval 8 s solo de red de seguridad; fn SIEMPRE idempotente). Al tocar acg-*.js, subir `?v=` en TODAS las páginas (los /assets/*.js llevan max-age=300).

## Errores a no repetir (add 2026-07-16c) — flash de FOTOS demo al cambiar src
- El esqueleto tapa el TEXTO demo pero las IMÁGENES demo se descargan igual bajo él; al llegar el feed, el runtime cambia el src del MISMO <img> y el navegador sigue mostrando la foto vieja hasta que baja la nueva → "texto real con foto demo" un instante (segundos en red lenta). Los detectores de texto (innerText) dan limpio: hay que mirar document.images/currentSrc. REGLA: mientras remoteStatus!=='ready', todo imgSrc alimentado por demo se sustituye por el píxel BLANK (data:image/gif;base64,R0lGODlhAQABAAAAACH5BAEKAAEALAAAAAABAAEAAAICTAEAOw==) → la transición visible es siempre gris→real, nunca foto→foto.

## Errores/preferencias — mensajes nativos (2026-07-13)
- NUNCA usar alert()/confirm()/prompt() nativos en la web. La web debe comunicar con SU PROPIA UI (toast/modal con estilo del sitio). En admin.mjs ya existen helpers askConfirm(), askPrompt() y toast() → usarlos siempre. Detectado en el "copiado" del enlace de afiliados.

## Afiliados: legacy + reafiliación + cambiar cuota (2026-07-13)
- Tabla `afiliados` (23 del sistema manual) NO se toca. renderAfiliados muestra 2 bloques: activos Stripe + "Afiliados anteriores (pendientes de reafiliación)". Match por nombre normalizado (NFD, sin acentos, upper): cuando un anterior se afilia por el enlace con el mismo nombre, sale de "anteriores" y aparece arriba como socio Stripe. Su total aportado histórico es solo informativo (NO suma en acumulados; ya se sumó en su día).
- Nueva acción admin `afiliado-cambiar-cuota`: crea Price nuevo (mismo product) y actualiza el subscription item, proration_behavior=none.

## Bug propuesta duplicada + sistema de bloqueo (2026-07-14)
- DUPLICADO propuestas: CrearPropuesta cargaba support.js Y assets/acg-forms.js, ambos enganchaban submit de #acg-form-propuesta → doble POST /api/propuesta. Fix: desactivado el handler de support.js (el bueno con imágenes es acg-forms.js). REGLA: un formulario = un solo handler; no montar el mismo listener en dos scripts globales.
- BLOQUEO miembros: requiere SQL (columnas profiles.bloqueo_motivo, bloqueado_at, apelacion_texto, apelacion_at). Backend: user-block con motivo+email (email desde auth admin API, profiles NO guarda email), bloqueados-list, apelar (en cuenta.mjs). Enforcement bloqueado en chat.mjs (ya), comentarios.mjs y propuesta.mjs. Frontend: admin modal askBlockReason (5 motivos+propio) + sección "Miembros bloqueados" en Comunidad; área ciudadana muestra cartel BLOQUEADO wanted + tachadura diagonal + botón apelar.
- Propuestas rechazadas: acciones prop-archivar (estado 'archivada') y prop-eliminar (borra comments+votes+proposal) + sección en renderModeracion.

## Mapas (reglas permanentes, 2026-07-17)
- **Leaflet SIEMPRE con `defer`** en el `<head>` (sin defer bloquea el primer render de la página entera; pasó en CrearPropuesta y el usuario lo notó).
- **Inyección de mapas sincronizada al render** (MutationObserver + intervalo de respaldo), nunca solo polling: el mapa debe aparecer A LA VEZ que el formulario, no 1,5 s después. Tras crear el mapa: `invalidateSize()` a 250/800/1800 ms + IntersectionObserver (si no, tiles grises hasta hacer scroll).
- **UN solo criterio de búsqueda para TODOS los mapas** (admin y público): Photon (tolerante, multiidioma) con sesgo lat/lng de Gandia + variantes ES↔VA por diccionario (calle/carrer, parque/parc, estación/estació…) + filtro de caja (lat 38.84–39.12, lng −0.36–−0.01) + fallback Nominatim anclado "…, Gandia, Valencia". Mapa nuevo con búsqueda → reutilizar `geoBuscar()` (admin) / `buscar()` (acg-forms), no inventar otro.
- El CSP de páginas públicas debe permitir `connect-src` a photon.komoot.io y nominatim.openstreetmap.org (sin eso el buscador falla en silencio; fue el bug original de /crear-propuesta).
